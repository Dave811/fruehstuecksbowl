{
  "name": "fruhstucksbowls-bestellsystem",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"bun run dev --filter frontend\" \"bun run dev --filter backend\"",
    "dev:frontend": "bun run --filter frontend dev",
    "dev:backend": "bun run --filter backend dev",
    "build": "bun run build --filter frontend && bun run build --filter backend",
    "install:all": "bun install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
